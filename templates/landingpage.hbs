<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Innovation Tracker</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(270deg, #ffffff, #eef2f6, #d7e6f9);
            overflow-x: hidden;
            scroll-behavior: smooth;
            
        }

        header {
            background-size: cover; 
            background-position: center;
            color: white;
            padding: 100px 0;
            background: linear-gradient(to bottom, #ffffff, #a8ceec);
            clip-path: polygon(0 0, 100% 0, 100% 75%, 0% 100%);
            display: flex;
            justify-content: space-around;
            align-items: center;
            text-align: start;
        }

        header h1 {
            font-size: 3.5rem;
            font-weight: 700;
            color: rgb(255, 255, 255);
            background: linear-gradient(135deg, #4f93b5, #574d4d);
            background-clip: text;
            -webkit-background-clip: text;
            color: transparent;
            -webkit-text-fill-color: transparent;
            animation: fadeIn 2s ease-in-out;
        }

        header p {
            font-size: 1.2rem;
            margin-top: 15px;
            animation: fadeIn 3s ease-in-out;
            color: #3e3e3e;
        }

        header button {
            margin-top: 25px;
            font-size: 1.2rem;
            padding: 10px 25px;
            background: linear-gradient(to right, #6eb4ff, #4a90e2);
            border: none;
            border-radius: 25px;
            color: white;
            transition: all 0.5s;
            animation: fadeInUp 4s ease-in-out;
        }

        header button:hover {
            background: linear-gradient(to left, #6eb4ff, #4a90e2);
            transform: translateY(-5px);
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        section {
            padding: 80px 0;
            opacity: 0; /* Start hidden */
            transform: translateY(50px); /* Start slightly below */
            transition: opacity 0.5s ease, transform 0.5s ease;
        }

        section.visible {
            opacity: 1; /* Fade in */
            transform: translateY(0); /* Move to original position */
        }

        section h2 {
            text-align: center;
            font-weight: bold;
            margin-bottom: 40px;
        }

        .about-us {
            background: linear-gradient(180deg, #d7e6f9, #eef2f6);
            padding: 50px;
            border-radius: 15px;
        }

        .services-card {
            transform: scale(1);
            transition: transform 0.5s, box-shadow 0.5s;
            border: 1px solid #d1e6f9;
            border-radius: 15px;
            overflow: hidden;
            background: #fff;
            padding: 20px;
            text-align: center;
            margin: 1rem;
        }

        .services-card:hover {
            transform: scale(1.05);
            box-shadow: 0 15px 25px rgba(0, 0, 0, 0.2);
        }

        .testimonials {
            display: flex;
            flex-wrap: wrap;
            gap: 30px;
        }

        .testimonial-card {
            background: white;
            padding: 20px;
            border-radius: 10px;
            border: 1px solid #eef2f6;
            box-shadow: 0 10px 15px rgba(0, 0, 0, 0.1);
            flex: 1;
            min-width: 300px;
        }

        .testimonial-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 25px rgba(0, 0, 0, 0.2);
        }

        .faq .question {
            cursor: pointer;
            background: linear-gradient(to left, #8cbae7, rgb(225, 218, 218));
            padding: 15px;
            border-radius: 5px;
            /* margin-bottom: 20px; */
            margin-top: 20px;
            transition: all 0.3s;
        }

        .faq .answer {
            display: none;
            margin-bottom: 40px;
            padding: 10px;
            background: #f9f9f9;
            border-left: 4px solid #4a90e2;
        }

        .faq .question:hover {
            background: #d7e6f9;
        }

        .contact-form input, .contact-form textarea {
            border-radius: 8px;
            border: 1px solid #ced4da;
            padding: 10px;
            margin-bottom: 10px;
            width: 100%;
        }

        .contact-form button {
            padding: 10px 20px;
            border: none;
            background: #4a90e2;
            color: white;
            border-radius: 25px;
            transition: all 0.3s;
        }

        .contact-form button:hover {
            background: #357ab8;
        }

        #scroll-to-top {
            position: fixed;
            bottom: 20px;
            right: 20px;
            z-index: 50;
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: linear-gradient(to right, #4a90e2, #6eb4ff);
            color: white;
            cursor: pointer;
            display: flex;
            justify-content: center;
            align-items: center;
            transition: transform 0.3s;
        }

        #scroll-to-top:hover {
            transform: scale(1.1);
        }

        .services-row {
            display: flex;
            justify-content: space-between; /* Initial spacing */
            margin-bottom: 50px;
            opacity: 0; /* Initially hidden */
            transform: translateY(50px); /* Initially shifted down */
            transition: transform 0.8s ease-out, opacity 0.8s ease-out;
        }

        .services-row.visible {
            opacity: 1; /* Fade in */
            transform: translateY(0); /* Final position */
            justify-content: space-around; /* Final horizontal spacing */
        }
        
  .modal1{
    position: fixed;
    width: 40%;
    height: 55%;
    left: 30%;
    top: 35%;
    z-index: 2;
    display: none;
  }
  .modal1 > .modal-content1{
    background-color:#faf0f765;
    backdrop-filter: blur(10px);
    color: #3558a4;
    border-radius: 10px;
    border: 1px solid rgba(99, 99, 248, 0.336);
    display: flex;
    flex-direction: column;
    width: 100%;
    padding-bottom:0.25rem ;
    box-shadow: inset 0 0 0.25rem black;
    height: 100%;
  }
  .modal-content1 > button{
    background: linear-gradient(to right,rgb(160, 219, 229),rgb(130, 130, 226));
    border-radius: 5px;
    padding: 10px 20px;
    margin: 20px;
    font-size: 20pt;
  }
  .modal-content1 > p{
    font-weight: bold;
    text-align: center;
    text-shadow: 2px 2px 3px rgb(112, 112, 112);
    font-size: 24pt;
    padding-top: 50px;
  }
  .close1{
    position: absolute;
    right: 1rem;
    border: none;
    background-color: transparent;
    font-size: xxx-large;
    font-weight: bold;
  }
  .close1:hover{
    color: rgb(223, 4, 4);
    transition: all ease 350ms;
    cursor: pointer;
    background-color: rgb(250, 100, 100);
  }
  .logo
  {
    font-weight: 900;
    font-size: 2rem;
    padding: 1rem;
  text-shadow: 0 0 0.2rem rgb(176, 169, 169);
      color: #3558a4;
  letter-spacing: 3px;
  }
  .logoandnav{
    background:linear-gradient(rgb(225, 208, 177),rgb(168, 204, 220));
  }

@media screen and (max-width: 880px) {
  body { header h1{
font-size: 2rem;
  }
  header p{
    font-size: smaller;
  }
header img{
    height: 80%;
    width: 100%;
}
  }
}

    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow fixed-top">
        {{!-- ////////// --}}
        <div id="loginRoleModal" class="modal1">
    <div class="modal-content1">
      <span ><button class="close1" >&times;</button></span>
      <p>Login As:</p>
      <button onclick="location.href='signinUI'">Student</button>
      <button onclick="location.href='Staffsignin'">Faculty</button>
      <button onclick="location.href='Adminsignin'">Admin</button>
    </div> 
  </div>
  {{!-- ////////////// --}}
        <div class="container "  >
            <a class="navbar-brand fw-bold text-primary logo" href="/landingpage">Innovatia</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link text-primary" href="#about">About Us</a></li>
                    <li class="nav-item"><a class="nav-link text-primary" href="#services">Services</a></li>
                    <li class="nav-item"><a class="nav-link text-primary" href="#testimonials">Testimonials</a></li>
                    <li class="nav-item"><a class="nav-link text-primary" href="#faq">FAQ</a></li>
                    <li class="nav-item"><a class="nav-link text-primary" href="#footer">Contact</a></li>
                </ul>
                  <button  id="loginbtn" class="btn btn-primary ms-3">Login</button>
              </div>
        </div>
    </nav>

    <header style="display: flex; justify-content: space-evenly;">
        <div class="container" style="text-align: center; padding: 20px;">
            <h1>Track, Showcase and Innovate Student Projects</h1>
            <p>"A platform dedicated to capturing and promoting student-driven innovations."</p>
            
      <a href="./signupUI"><button id="getStartedBtn">Get Started</button></a>
        </div>
        <div class="hero">
<img src="/img/heroimg.png" alt="">
        </div>
    </header>

    <section id="about">
        <div class="container">
            <h2>About Us</h2>
            <div class="about-us">
                <p>Innovatia is a leading platform designed to nurture creativity and innovation among students. We aim to help students track their innovative projects, collaborate effectively, and gain the recognition they deserve.</p>
            </div>
        </div>
    </section>

    <section id="services">
        <div class="container">
            <h2>Our Services</h2>
            <div class="row">
                <div class="col-md-4">
                    <div class="services-card">
                        <i class="bx bx-bulb text-primary fs-1"></i>
                        <h5>Innovation Tracking</h5>
                        <p>Track the progress of your innovative ideas with our seamless tools.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="services-card">
                        <i class="bx bx-show text-primary fs-1"></i>
                        <h5>Project Showcase</h5>
                        <p>Present your projects professionally with our intuitive interface.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="services-card">
                        <i class="bx bx-group text-primary fs-1"></i>
                        <h5>Collaboration Tools</h5>
                        <p>Collaborate with peers and mentors on groundbreaking ideas.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section id="testimonials">
        <div class="container">
            <h2>What People Say</h2>
            <div class="testimonials">
                <div class="testimonial-card">
                    <p>"Innovatia has transformed how I approach my innovative projects. It's a game-changer!"</p>
                    <small>- John Doe</small>
                </div>
                <div class="testimonial-card">
                    <p>"Thanks to Innovatia, I secured a scholarship for my innovation!"</p>
                    <small>- Jane Smith</small>
                </div>
            </div>
        </div>
    </section>

    <section id="faq">
        <div class="container">
            <h2>Frequently Asked Questions</h2>
            <div class="faq">
                <div class="question">What is Innovatia?</div>
                <div class="answer">Innovatia is a platform that tracks and showcases student innovations.</div>

                <div class="question">How can I use Innovatia?</div>
                <div class="answer">Sign up, start adding projects, and track your progress with intuitive tools.</div>
            </div>
        </div>
    </section>

    <footer id="footer" class="text-center py-4 text-white" style="background-color: #2f363e;">
        <p>&copy; 2024 Innovatia. All Rights Reserved.</p>
    </footer>

    <button id="scroll-to-top" onclick="scrollToTop()">↑</button>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function scrollToTop() {
            window.scrollTo({ top: 0, behavior: 'smooth' });
        }

        document.querySelectorAll('.faq .question').forEach(q => {
            q.addEventListener('click', function () {
                const answer = this.nextElementSibling;
                answer.style.display = answer.style.display === 'block' ? 'none' : 'block';
            });
        });

        document.addEventListener("DOMContentLoaded", function() {
            const sections = document.querySelectorAll('section');

            const observerOptions = {
                root: null,
                threshold: 0.5
            };

            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('visible');
                        observer.unobserve(entry.target);
                    }
                });
            }, observerOptions);

            sections.forEach(section => {
                observer.observe(section);
            });
        });
        

const loginbtn=document.querySelector("#loginbtn");
const formlogin=document.querySelector("#loginRoleModal")
loginbtn.addEventListener("click",(eve)=>{
formlogin.style.display="flex";
})
const crossbtn=document.querySelector(".close1");
crossbtn.addEventListener("click",()=>{
formlogin.style.display="none";
    
})
// Get modal and button elements
const regmodal = document.getElementById('regRoleModal');
const getStartedBtn = document.getElementById('getStartedBtn');
const closeBtn = document.querySelector('.close');

// Open modal on "Get Started" button click
getStartedBtn.addEventListener('click', () => {
  regmodal.style.display = 'flex'; // Show the modal
});

// Close modal on clicking the close button
closeBtn.addEventListener('click', () => {
  regmodal.style.display = 'none'; // Hide the modal
});

// Close modal on clicking outside the modal content



        
    </script>
</body>
</html>